# Author: Nuncvc1v0

# Get monitor name: hyprctl monitors all

# Built-in laptop display: 1920x1080@60Hz, no scaling
monitor = eDP-1, 1920x1080@60, 0x0, 1

# External monitor on the right (1080p)

# External monitor 1440p on the right

# External monitor 4K on the right with scaling

# External monitor on the left

# DisplayPort monitor

# Fallback for unknown monitors (auto-configure)
monitor = , preferred, auto, 1

# Virtual machines (QEMU, VirtualBox, VMware)

# Disable specific monitor

# MIRROR MODE (duplicate displays)

# Mirror laptop display to HDMI
